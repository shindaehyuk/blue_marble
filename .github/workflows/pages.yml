name: Deploy GitHub Pages

# master에 머지(푸시)되면 gh-pages 브랜치로 미러링 → GitHub Pages가 자동 배포
on:
  push:
    branches: [master]
  workflow_dispatch:

permissions:
  contents: write

concurrency:
  group: pages
  cancel-in-progress: true

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - name: Mirror to gh-pages
        run: git push origin HEAD:gh-pages --force
